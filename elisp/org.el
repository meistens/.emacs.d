;;; org.el --- lexical-binding: t -*-
;;; commentary:
;;; code:

;; org
;; (defun org-mode-setup ()
;;  (variable-pitch-mode 1)
;;  (visual-line-mode 1))

;;  (use-package org
;; :hook (org-mode . org-mode-setup)
;;  :config
(setq org-ellipsis " ▾")

(provide 'org)
;;; org.el ends here
